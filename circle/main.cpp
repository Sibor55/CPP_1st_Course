#include <iostream>
#include <vector>
#include <Windows.h>
#include <thread>
#include <chrono>
#include <string>

// BOOL setCursorToPosition(int x, int y) {

//         COORD c;
//         c.X = x;
//         c.Y = y;
//         return SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE), c);
// }

// bool isKeyPressed(int virtKey){
//     return GetAsyncKeyState(virtKey) & 0x8000;
// }


int main() {



    // int x = 0, y = 0;
    
    // setCursorToPosition(33,7);



    // while(true){
    //     if (isKeyPressed(VK_LEFT)) 
    //         --x;
    //     else if(isKeyPressed(VK_RIGHT))
    //         ++x;
            
    //     else if (isKeyPressed(VK_DOWN)) 
    //         ++y;

    //     else if(isKeyPressed(VK_UP))
    //         --y;
            
    //     else if(isKeyPressed(VK_ESCAPE))
    //         break;
    //     std::this_thread::sleep_for(std::chrono::milliseconds(5));
    //     setCursorToPosition(10+x,10+y);
    //     std::cout << "*";

    
    }

    return 0;
}